import "./Loading.css";

const Loading = () => {

  return (
    <main className="content-spinner">
            <h5>Loading...</h5>
        <div className="loading-spinner">
        </div>
    </main>

  );
};

export default Loading;
